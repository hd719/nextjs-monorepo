// Authentication client (Better Auth)
export { authClient, signIn, signUp, useSession } from "./auth-client";

// Authentication server config (Better Auth)
export { auth, type Session } from "./auth-config";

// Database client (Prisma)
export { prisma } from "./prisma";
